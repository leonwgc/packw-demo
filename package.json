{
  "name": "packx-demo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clear": "rimraf ./dist && rimraf ./t-dist & clear",
    "start": "packx start",
    "build": "packx build",
    "start:node": "node packx --dev --env test --cfg",
    "build:node": "node packx --env test --cfg",
    "start:h5": "target=h5 packx start h5",
    "start:pc": "target=pc packx start pc",
    "build:h5": "target=h5 packx build h5",
    "build:pc": "target=pc packx build pc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "antd": "^4.15.6",
    "antd-form-render": "^1.1.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "zarm": "^2.9.0",
    "zarm-form-render": "^1.1.4"
  },
  "devDependencies": {
    "packx": "~0.4.0",
    "postcss-px-to-viewport": "^1.1.1",
    "rimraf": "^3.0.2"
  }
}
